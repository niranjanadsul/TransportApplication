spring:
  application:
    name: transport-app

  datasource:
    url: jdbc:mysql://localhost:3306/transport_db
    username: root
    password: Admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
